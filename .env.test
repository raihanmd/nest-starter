# Test Database Configuration
# Used when NODE_ENV=test (automatically loaded for E2E tests)

DATABASE_URL_TEST=postgres://postgres:root@localhost:5435/nest_starter_test?schema=public

# Server Configuration
SERVER_PORT=3001

# JWT Configuration
JWT_SECRET=test-jwt-secret-change-in-production

# API Key (for middleware)
API_KEY=test-api-key-change-in-production
